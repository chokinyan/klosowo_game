#include "ai/ai_model/elagage.h"

int eval()
{
    int score = 0;

    return score;
}

int min_max(  int depth, int heuristic, int alpha, int beta )
{
    if ( depth == 0 )
        return heuristic;

    return 0;
}
